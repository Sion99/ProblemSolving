#include <stdio.h>

void recursive(int x, int y, int *result)
{
    if (x )
}

int main()
{
    int n, x, y;
    int result;

    scanf("%d %d %d", &n, &x, &y);
    result = 0;
    recursive(x, y, &result);
    printf("%d\n", result);
}