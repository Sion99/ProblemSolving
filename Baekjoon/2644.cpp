#include <iostream>
#include <queue>

using namespace std;

int main()
{
    int n, a, b, m, x, y;
    cin >> n >> a >> b >> m;
    for (int i = 0; i < m; i++)
    {
        cin >> x >> y;
        }
}